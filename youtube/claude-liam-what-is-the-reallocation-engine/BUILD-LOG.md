# BUILD-LOG — claude-liam-what-is-the-reallocation-engine

## 2026-08-23 — PLAN authored (gate presented, awaiting approval)

- Family: **ai-explainer** (claude-liam · Kokoro am_onyx · Teardown ·
  IN-FOR-BEAR law honored at B00 and BOUT). Episode 1 of the playlist
  "The Reallocation Engine" — the visual README, context for every later video.
- Greeting: "Hola, Liam" (rotation: Annyeong used by the git-claude-code reel,
  Namaste reserved by the parked deep episode).
- 16 beats, est 4:29 (805 words ≈ 4:38 at 2.9 w/s — audio will be the clock).
- Spine: cold open (question = the title, answered) → thesis → fluency trap →
  stakes → the name → three mechanisms (contract / gates / skip rate) →
  phase-gate handback → ASK→RESULT quick start (verbatim README commands) →
  repo map → series roadmap → verdict → your-turn → title outro.
- ILLUSTRATE law: UI only at B00, BASK1, BVDT, BHTF, BOUT. Typing only at
  B00 + BHTF. No two consecutive beats share a visual scheme.
- C2 beats (B04 divergence, B06 threshold) need `anim.json` + pattern-table
  sign-off at build (animated-deck gate). C3 beats start from the
  illustrations library (SourceFlow ×2, PredictCard, LayerStack, ChipGrid) —
  adapt props, log prop changes here.
- One deliberate tension logged: B03 keeps the README's own "sixty to ninety
  days" phrasing (source-faithful) rather than the flat 90-day OPT limit —
  if the README line is ever tightened, update this beat with it.

### Gate status

| Gate | Status |
|---|---|
| PLAN | **presented — awaiting approval** |
| anim.json (C2 table) | pending, at build |
| GATE P (narration) | pending |
| audio → conform | pending |
| VISUAL QC | pending |

### MISSING / machine-side

- Manim scenes: B02_FluencyTrap, B03_TheClock, B07_SkipDial (specs in
  `graphic.production_viz`).
- Illustration prop adaptation for SourceFlow/PredictCard/LayerStack/ChipGrid
  against the installed library.
- Thumbnail suggestion: the B01 frame (book → terminal → decision card) with
  the title serif — the episode's thesis in one image.
