# BUILD-LOG — claude-liam-reallocation-engine

## 2026-08-18 — PLAN authored (Gate 1 presented, awaiting approval)

- Family: **deep-explainer** (extends ai-explainer → explainer; closing block
  per your-turn standard).
- Channel `claude-liam` (Kokoro `am_onyx`, free — IN-FOR-BEAR LAW: Liam signs
  in at B00 and signs off at BOUT). Greeting: "Namaste, Liam" (world-language
  rotation; Annyeong was used by a recent exemplar).
- Title: **"The Engine That Says No."** — restated verbatim at BOUT (OUTRO LAW).
- Structure: cold open + exec summary, 5 acts (Volume Trap · Evidence First ·
  Gates Not Weights · Authorization Trap · Forged in a Classroom), your-turn
  closing block. 33 beats; 27 body beats.
- **Lane histogram (body):** VOX 6 (22.2%) ✓ in band · MANIM 7 (25.9%) ·
  REMOTION 8 (29.6%, a hair under the 30–45 guide — acceptable; the two ask
  micro-beats are exempt but render as Remotion) · CARD 6 (22.2%). No
  3-streaks outside the vox run.
- **Vox run:** R1 = B05→B06 (calendar macro → zoom-out to desk + document),
  handoff block authored (camera 0.5/0.42 @1.45). All other lane boundaries
  are hard cuts, per the continuity contract.
- Estimated 7:10 — inside the 5–10 band; duration is an output, the measured
  audio will be the clock.
- ASK→RESULT pairs: BASK1→B10 (provenance output), BASK2→B25 (harness suite).
  All code beats are Onda code-blocks with VERBATIM repo source (B10 shape from
  the scorer's trace; B16 and B21 quoted line-for-line; B25 real executed
  harness output).
- Honesty decisions: the negation-blind bug is IN the film (B21) presented as
  a documented open finding — per rule 5, FACTCHECK.md will mark it
  independently reproduced, not inherited. No dates, no student names, no real
  personal data; persona beats carry the FICTIONAL chip on screen.
- PII note: every planned VOX still is generic/staged (tier 1) — no real
  person identifiable, no real documents. Tier assignments in the sheet's
  `pantry` blocks; final tiering at Gate D2.

### Gate status

| Gate | Status |
|---|---|
| 1 · PLAN | **presented — awaiting human approval** |
| 2 · FACTCHECK | pending (after plan approval) |
| P · narration sign-off | pending |
| audio lock | pending |
| D2 · SHOPPING.md | ONLY after audio lock (hard rule 3) |
| D1 · slate previz | pending (`./art run` on the toolkit machine) |

### MISSING / machine-side steps (run on the Mac, from `books/`)

- MISSING: `pantry/` stills for B03 B05 B06 B11 B22 B26 (6 slots) — tier-0
  library pass (`pantry_search.py`) first, then SHOPPING.md, both after audio
  lock.
- MISSING: Manim scenes B04 B09 B13 B14 B17 B20 B24 (specs in each beat's
  `graphic.production_viz`).
- MISSING: C2/C3 prop verification against the installed pattern/illustration
  libraries (LayerStack / SourceFlow / PredictCard / deckPatterns names used;
  adapt props at build time — log any retint/prop changes here).
