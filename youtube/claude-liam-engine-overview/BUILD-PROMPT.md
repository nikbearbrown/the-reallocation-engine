# BUILD-PROMPT — claude-liam-engine-overview

Paste this into a fresh Claude Code session (in `books/`) to rebuild this reel end-to-end.

---

## Context

Reel: **claude-liam-engine-overview**  
Book: `the-reallocation-engine-fresh/`  
Reel path: `the-reallocation-engine-fresh/youtube/claude-liam-engine-overview/`  
Skill: `deep-explainer` (5–10 min, all gates, Liam persona, Kokoro am_onyx)  
Channel: `claude-liam` — cream/terracotta Claude brand palette, free audio only  
Kind: `engine` — teach the whole machine design

## Source files (read-only)

Read these before building. Never edit them:
- `the-reallocation-engine-fresh/README.md`
- `the-reallocation-engine-fresh/DOMAIN.md`
- `the-reallocation-engine-fresh/DATA_CONTRACT.md`
- `the-reallocation-engine-fresh/AGENTS.md`
- `the-reallocation-engine-fresh/package.json`

## Constraints

- VOX quota waived to zero — no pantry stills, no SHOPPING.md entries, all beats REMOTION
- Never publish, upload, or touch `books/youtube/TOPOST/`
- Never spend money (Kokoro audio is free and local)
- Never edit source files outside `youtube/claude-liam-engine-overview/`
- No questions — log every decision in `BUILD-LOG.md` and proceed

## Central claim

"The Reallocation Engine is a filter, not a finder. Its value is in the applications it talks you out of. Skip is the success case."

BrutalistHesitantWriter B01: `triggerWords: "find"`, `replacementWords: "stop"` (seed must be a STRING, not a number)

## Beat spine (20 beats)

```
B00  ClaudeComposerAsk     cold open — Liam signs in, "this is Liam, in for Bear"
B01  BrutalistHesitantWriter  BLUF — "find" crossed out → "stop" (seed: "7" — string!)
B02  SkillTeardownMechanism   Act I: The Fluency Trap
B03  BarChart              skip majority (52/100) as success metric
B04  PipelineFlow          five components, two gates labeled
B05  SkillTeardownMechanism   SEC Form D
B06  SkillTeardownMechanism   80 Days sponsorship
B07  PipelineFlow          ATS liveness hard gate (multiply-by-zero)
B08  BarChart              BLS 100/112 metro-SOC pairs coverage
B09  SkillTeardownMechanism   Visa timeline hard gate
B10  FormACard             act card: Act III — The Composite Score
B11  ClaudeCodeBeat        Bayesian formula (title: "scripts/score/role-scorer.mjs")
B12  BarChart              worked example: 0.446 Apply vs 0.178 Skip
B13  FormACard             act card: Act IV — Recipes and the Human Gate
B14  PipelineFlow          recipe lifecycle → agent → gate → human sign
B15  ClaudeCodeBeat        command surface (title: "package.json", real JSON scripts)
B16  SkillTeardownMechanism   what the machine won't do / human judgment
BVDT ClaudeVerdictArtifact   verdict: five gates, one decision
BHTF ClaudeComposerAsk        handoff: "Your turn." / clone and run
BOUT ClaudeTitleOutro         "Liam, in for Bear."
```

## Known GATE T rules for SkillTeardownMechanism body field

§8.5 no-wordy-card: `body` must be ≤12 words. Pull-quotes, not sentences. Keep body to 6–10 words.

## Known Zod schema notes

- `BrutalistHesitantWriter.seed` must be a **string** (e.g., `"7"`), not a number
- `ClaudeCodeBeat.title` is the language badge — must include a file extension (e.g., `"package.json"`)
- `ClaudeCodeBeat.code` must contain real code tokens (not comment-only lines)
- `shot.remotion.pattern` must be INSIDE `shot` (i.e., `shot.remotion.pattern`), NOT at beat top level

## Build commands (from `books/`)

```bash
# 1. Generate audio (if not already done):
python3 brutalist-art/runtime/scripts/generate_audio_kokoro.py \
  the-reallocation-engine-fresh/youtube/claude-liam-engine-overview

# 2. Review cut (re-renders only missing beats):
ART_STRICT=0 ./brutalist-art/art run the-reallocation-engine-fresh/youtube/claude-liam-engine-overview

# 3. Clean master (runs GATE T — fix any TYPECHECK.md FAILs first):
./brutalist-art/art final the-reallocation-engine-fresh/youtube/claude-liam-engine-overview
```

## Required files

```
beat_sheet.json       master (remotion inside shot, not at beat top level)
FACTCHECK.md          | # | Beat | Claim (as spoken / shown) | Verdict | Source | Fix |
SHOPPING.md           empty (VOX=0 waived — one sentence explaining why)
scenes.py             minimal stub (imports from manim, no Manim scene classes)
BUILD-LOG.md          decisions, errors, fixes, sourcing
BUILD-PROMPT.md       this file
TYPECHECK.md          generated by art final — must show no FAILs before shipping
```

## Supervisor checks

```bash
# mp4 newer than beat_sheet.json:
ls -lt the-reallocation-engine-fresh/youtube/claude-liam-engine-overview/*.mp4 \
       the-reallocation-engine-fresh/youtube/claude-liam-engine-overview/beat_sheet.json

# Mean volume above -40 dB:
ffprobe -v quiet -show_entries format_tags=r128_track_gain \
  -of default=noprint_wrappers=1 \
  the-reallocation-engine-fresh/youtube/claude-liam-engine-overview/claude-liam-engine-overview.mp4

# narration lint clean:
python3 brutalist-art/runtime/scripts/narration_lint.py \
  the-reallocation-engine-fresh/youtube/claude-liam-engine-overview
```
