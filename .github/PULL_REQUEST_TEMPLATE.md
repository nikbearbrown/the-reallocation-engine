## What this PR adds

<one paragraph — component name, which engine layer, what it verifies>

Branch: `contrib/<term>-<handle>-<component>` · This is my ONLY open PR: [ ] yes
Maintained file I deliberately patch (or "none"): `____`

## Checklist (CI re-checks every box; an unchecked box is an auto-request-changes)

- [ ] `npm run doctor` clean locally — paste the last 3 lines below
- [ ] **No real PII anywhere in this branch's history** — fictional personas
      from `search/examples/` only (DATA_CONTRACT §Zero-Conditions). If I ever
      committed real data, I re-cut the branch; deleting the file later is NOT enough.
- [ ] Run log added as `logs/runs/<term>-<handle>-<n>.md`; I did NOT edit `logs/RUN_LOG.md`
- [ ] Recipe + card pair under `recipes/cases/<term>/` with honest lifecycle
      frontmatter (status matches evidence; attestation only if a human signed)
- [ ] My harness/tests are green — real output pasted below, not described
- [ ] Diff touches only my namespaced paths (+ the one declared maintained file)
- [ ] `npm run verify` passes locally

## Doctor output (last 3 lines, pasted)

```
```

## Harness output (pasted, not described)

```
```
