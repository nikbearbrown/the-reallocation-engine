# .ai/manifest.yaml — machine-readable twin of _MANIFEST.md
# Lets scripts, CI, and validators act on the same map without parsing prose.
# Keep in sync with _MANIFEST.md. SNICKERDOODLE.md governs all conflicts.
name: the-reallocation-engine
governed_by: SNICKERDOODLE.md
domain_map: DOMAIN.md
# All generated from instructions/ by scripts/build-instructions.mjs (do not hand-edit).
# AGENTS.md is canonical; the rest are thin shims that point at it (no rule duplication).
generated_instructions:
  - AGENTS.md
  - CLAUDE.md
  - .gemini/settings.json
  - .aider.conf.yml
  - .github/copilot-instructions.md
  - .cursor/rules/reallocation-engine.mdc

canonical:
  - SNICKERDOODLE.md
  - DOMAIN.md
  - AGENTS.md
  - PROJECT_RULES.md
  - DATA_CONTRACT.md
  - status.md
  - logs/RUN_LOG.md

task_relevant:
  - recipes/
  - scripts/
  - book/
  - data/
  - docs/
  - instructions/
  - eval/
  - session-handoff.md

ignore:
  - output/
  - reports/generated/
  - node_modules/
  - "**/__pycache__/"
  - "*.pyc"
  - .build/
  - archive/

# Private by default — reveals personal job-search activity. Gitignored AND never committed.
# `private/` holds the student's real data (real résumé, real tracker); only private/README.md is tracked.
# `npm run doctor` fails if any path here is git-tracked (see the privacy check in scripts/doctor.mjs).
private:
  - private/            # real résumé, real ATS tracker, real contacts (only README.md tracked)
  - data/ats/
  - "**/*.resume.pdf"
  - resumes/**/*.pdf
  - resume.json
  - .env
  - .env.*

entrypoints:
  verify: "npm run verify"
  doctor: "npm run doctor"
  build_instructions: "node scripts/build-instructions.mjs"
  conformance: "node scripts/conformance.mjs"

invariants:
  - "SNICKERDOODLE.md governs; conflicts are bugs logged in logs/RUN_LOG.md."
  - "Never invent a count, rate, or confidence; label model judgments as judgments."
  - "Gates are hard stops cleared by a named human and logged."
  - "Never delete source, data, recipes, or logs — archive instead. Safe rm: .build, __pycache__, *.pyc, *.bak."
  - "output/ and reports/generated/ are generated, never source."
  - "AGENTS.md and CLAUDE.md are generated from instructions/ — edit the source, then rebuild."

permissions:
  filesystem_scope: workspace-only
  private_paths_require_review_before_commit: true
  forbidden:
    - "rm -rf"
    - "git push --force"
